//write a programm to check if a given number is divisible by 5?

# include <stdio.h>
void main()
{
    int a;
    printf("entre the vlaue for a:");
    scanf("%d",&a);
    if(a%5==0)
    {
        printf("a is divisible by 5:");
    }
}